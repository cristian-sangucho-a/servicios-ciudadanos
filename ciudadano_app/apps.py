from django.apps import AppConfig

class CiudadanoAppConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'ciudadano_app'
    verbose_name = "Aplicación de Ciudadano"
